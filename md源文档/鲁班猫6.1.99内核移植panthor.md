# 0.准备工作
下载本次移植使用的6.1.99的内核：

`git clone -b lbc-develop-6.1 https://github.com/LubanCat/kernel.git`

下载可以在6.1上使用的panthor驱动：

`git clone https://github.com/jacobchencc/panthor-linux6.1.git`

本次移植参考的原文档：https://www.cnblogs.com/zyly/p/19188301#_label1
步骤有小部分需要修改，会在下面进行说明。

# 1.Panthor源码移植
## 1.1 `rk3588s.dtsi`文件修改
我们按照文档直接替换成`panthor-linux6.1`的文件：

`dev@localhost:~/self_linux/kernel-6.1$ cp /home/dev/self_linux/panthor-linux6.1/arch/arm64/boot/dts/rockchip/rk3588s.dtsi arch/arm64/boot/dts/rockchip/`

## 1.2 `rk3588-rk806-dual.dtsi`文件修改
鲁班猫系列的板卡，节点`vdd_gpu_s0`、`vdd_gpu_mem_s0`就在`rk3588-rk806-dual.dtsi`，所以我们直接修改这个文件的这两个节点：
```
vdd_gpu_s0: DCDC_REG1 {
		regulator-always-on; # 从regulator-boot-on修改成这个
		regulator-min-microvolt = <550000>;
		regulator-max-microvolt = <950000>;
		regulator-ramp-delay = <12500>;
		regulator-name = "vdd_gpu_s0";   
		regulator-enable-ramp-delay = <400>;
		regulator-state-mem {
				regulator-always-on; # 从regulator-off-in-suspend修改成这个
		};
};

vdd_gpu_mem_s0: DCDC_REG5 {
		regulator-always-on;  # 从regulator-boot-on修改成这个
		regulator-min-microvolt = <675000>;
		regulator-max-microvolt = <950000>;
		regulator-ramp-delay = <12500>;
		regulator-enable-ramp-delay = <400>;
		regulator-name = "vdd_gpu_mem_s0";
		regulator-state-mem {
				regulator-always-on;  # 从regulator-off-in-suspend修改成这个
		};
};
```
## 1.3 内核的defconfig文件修改
鲁班猫4的内核配置文件是`lubancat_linux_rk3588_defconfig`,所以我们也按照文档直接在这个文件最后添加：
```
CONFIG_DRM_PANTHOR=y
CONFIG_EXTRA_FIRMWARE="mali_csffw.bin"
CONFIG_EXTRA_FIRMWARE_DIR="./firmware"
```
## 1.4 `drivers/gpu/drm/Kconfig`文件修改
按照文档，直接在`drivers/gpu/drm/Kconfig`文件添加：
```
dev@localhost:~/self_linux/kernel-6.1$ vim drivers/gpu/drm/Kconfig

source "drivers/gpu/drm/panthor/Kconfig"
```
## 1.5 `drivers/gpu/drm/Makefile`文件修改
按照文档，直接在`drivers/gpu/drm/Makefile`文件添加：
```
dev@localhost:~/self_linux/kernel-6.1$ vim drivers/gpu/drm/Makefile 

# 22行替换成
                drm_managed.o drm_vblank_work.o \
                drm_exec.o drm_gpuvm.o
# 144行新增
obj-$(CONFIG_DRM_PANTHOR) += panthor/
```
## 1.6 `drm_debugfs`文件修改
直接参考文档把`panthor-6.1`里面的.c和.h复制到内核源码就行：
```
dev@localhost:~/self_linux/kernel-6.1$ cp /home/dev/self_linux/panthor-linux6.1/drivers/gpu/drm/drm_debugfs.c drivers/gpu/drm/


dev@localhost:~/self_linux/kernel-6.1$ cp /home/dev/self_linux/panthor-linux6.1/include/drm/drm_debugfs.h include/drm
```
## 1.7 `drm_gem`文件修改
这里同样把`panthor-6.1`里面的.c和.h复制到内核源码就可以：
```
dev@localhost:~/self_linux/kernel-6.1$ cp /home/dev/self_linux/panthor-linux6.1/drivers/gpu/drm/drm_gem.c drivers/gpu/drm/

cp /home/dev/self_linux/panthor-linux6.1/include/drm/drm_gem.h include/drm/
```
至于文档里还说要修改第`1053`行，我实测是不用的，改了反而会报错。
## 1.8 `include/drm/drm_drv.h`文件修改
直接参考文档复制过来就行：
```
dev@localhost:~/self_linux/kernel-6.1$ cp /home/dev/self_linux/panthor-linux6.1/include/drm/drm_drv.h include/drm/
```
## 1.9 `include/linux/io-pgtable.h`文件修改
这里原文档没有提到，但是也要复制到内核里，不然编译会报错：
```
dev@localhost:~/self_linux/kernel-6.1$ cp /home/dev/self_linux/panthor-linux6.1/include/linux/io-pgtable.h include/linux
```
## 1.10 新增部分
这里直接参考文档操作，复制多的文件，不用修改文件`drivers/gpu/drm/panthor/panthor_device.c`第`398`行：
```
dev@localhost:~/self_linux/kernel-6.1$ cp /home/dev/self_linux/panthor-linux6.1/drivers/gpu/drm/drm_exec.c drivers/gpu/drm/

dev@localhost:~/self_linux/kernel-6.1$ cp /home/dev/self_linux/panthor-linux6.1/include/drm/drm_exec.h include/drm/

dev@localhost:~/self_linux/kernel-6.1$ cp /home/dev/self_linux/panthor-linux6.1/drivers/gpu/drm/drm_gpuvm.c drivers/gpu/drm/

dev@localhost:~/self_linux/kernel-6.1$ cp /home/dev/self_linux/panthor-linux6.1/include/drm/drm_gpuvm.h include/drm/

dev@localhost:~/self_linux/kernel-6.1$ cp /home/dev/self_linux/panthor-linux6.1/include/uapi/drm/panthor_drm.h include/uapi/drm/

dev@localhost:~/self_linux/kernel-6.1$ cp -r /home/dev/self_linux/panthor-linux6.1/drivers/gpu/drm/panthor ./drivers/gpu/drm/
```
# 2.`CSF`固件移植
这里直接使用`panthor-6.1`的固件，不用特地去存储库下载：
```
dev@localhost:~/self_linux/kernel-6.1$ mkdir firmware/

dev@localhost:~/self_linux/kernel-6.1$ cp /home/dev/self_linux/panthor-linux6.1/firmware/mali_csffw.bin firmware/
```
# 3.编译内核
## 3.1编译过程
鲁班猫系列的镜像可以用deb包更新内核，所以把内核编译成deb包的形式，可以创建一个sh脚本，然后执行：
```
dev@localhost:~/self_linux/kernel-6.1$ vim mk_deb.sh 
```
```bash
#!/bin/bash

export CROSS_COMPILE=aarch64-linux-gnu-
make ARCH=arm64 lubancat_linux_rk3588_defconfig
make ARCH=arm64 bindeb-pkg RK_KERNEL_DTS=rk3588s-lubancat-4-v1.dts -j64
```
```
dev@localhost:~/self_linux/kernel-6.1$ sudo chmod +x mk_deb.sh

dev@localhost:~/self_linux/kernel-6.1$ ./mk_deb.sh 
```

## 3.2安装内核
编译完成后，能在内核同目录发现生成的deb包，如图：
![deb包名](./photo/gpuyizhi.png)
把这两个deb包复制到板卡，安装重启就可以。
# 4.桌面崩溃处理
我这里测试使用的板卡是鲁班猫4，镜像是debian12，进入桌面之后会提示崩溃，所以按照文档的方式修复：
```
cat@lubancat:~$ sudo mkdir /userdata
cat@lubancat:~$ sudo mv /etc/ld.so.conf.d/00-aarch64-mali.conf /userdata
cat@lubancat:~$ sudo ldconfig
cat@lubancat:~$ sudo reboot
```
# 5.mesa库问题
使用`export DISPLAY=:0.0; glxinfo -B`命令会发现现在还是使用cpu渲染的，没识别到gpu
```
# 卸载mali官方的deb包
 
sudo apt remove libmali-*
# 安装，更新mesa库
sudo apt update
sudo apt install aptitude
sudo aptitude install libd3dadapter9-mesa libd3dadapter9-mesa-dev libegl-mesa0 libegl1-mesa-dev libgbm-dev libgbm1 libgl1-mesa-dri libglx-mesa0 libosmesa6 libosmesa6-dev mesa-common-dev mesa-drm-shim mesa-libgallium mesa-opencl-icd mesa-teflon-delegate mesa-va-drivers mesa-vdpau-drivers mesa-vulkan-drivers -y

sudo apt install -y mesa-utils libgl1-mesa-dri libegl1-mesa libgles2-mesa xserver-xorg-video-modesetting

sudo apt install -t bookworm-backports mesa-utils libgl1-mesa-dri libegl1-mesa

# 加入黑名单，禁止加载mali_kbase
echo "blacklist mali_kbase" | sudo tee /etc/modprobe.d/blacklist-mali.conf
sudo update-initramfs -u

```
重启，再使用`export DISPLAY=:0.0; glxinfo -B`查看，会发现`Accelerated: yes`，这样就成功了。
# 6.需解决的问题
`sudo apt install firefox-esr`可以先安装一个火狐浏览器，但是看视频只能软解码
debian12自带的浏览器打不开，需要搞一个panthor的
